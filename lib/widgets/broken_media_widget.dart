import 'package:flutter/material.dart';

class BrokenMediaWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.broken_image, color: Colors.grey[400], size: 40.0);
  }
}
